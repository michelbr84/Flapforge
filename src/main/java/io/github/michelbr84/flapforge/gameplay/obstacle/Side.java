package io.github.michelbr84.flapforge.gameplay.obstacle;

/** The playfield edge a piston or a lightning bolt is anchored to (D6). */
public enum Side {
    /** Anchored to the top edge ({@code y = 0}). */
    TOP,
    /** Anchored to the ground line ({@code y = 598}). */
    BOTTOM
}
